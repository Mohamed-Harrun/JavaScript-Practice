// let newArray = array.map(function(element, index, array) {
//     // Return the value for the new array
// });

/* const numbers = [1,2,3,4];
function squares(Element){
    return Math.pow(Element,2);
}

const square = numbers.map(squares); // we only invokes the method by using objects



console.log(square); */


let numbers= [1,2,3,4];

function mul (value){
    return value*2; 
}

multiply = numbers.map(mul);
console.log(multiply);